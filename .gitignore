# Secrets — each user provides their own
config.json

# Python cache
__pycache__/
*.pyc

# Daemon auto-generated state and logs
state/
monitor_reports/

# Session notes (ARG narrative context, excluded from mirror)
.notes.md
